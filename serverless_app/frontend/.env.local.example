# API Configuration
# Replace with your deployed API Gateway URL after running: cd backend && npx serverless deploy
NEXT_PUBLIC_API_URL=https://your-api-gateway-url.execute-api.us-east-1.amazonaws.com
